{
  "$schema": "dpe/json/schema/library",
  "$libraries": {},
  "$body": { "$defRef": "recipes" },
  "$templates": {},
  "$definitions": {
    "recipes": {
      "$type": "array",
      "element": { "$ref": "utils:resource_location", "schema": "recipe" },
      "description": "The recipes that a player unlocks when this [ [knowledge book] ] is used."
    }
  }
}