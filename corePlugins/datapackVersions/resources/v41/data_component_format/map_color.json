{
  "$schema": "dpe/json/schema/library",
  "$libraries": {
    "utils": "../utils/utils-library.json"
  },
  "$body": { "$defRef": "map_color" },
  "$templates": {},
  "$definitions": {
    "map_color": {
      "$type": "union", "options": [{ "$defRef": "utils:int_color" }],
      "description": "The color of the markings on this filled map item texture."
    }
  }
}