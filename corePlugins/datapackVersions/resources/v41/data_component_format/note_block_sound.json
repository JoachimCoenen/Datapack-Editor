{
  "$schema": "dpe/json/schema/library",
  "$libraries": {
    "utils": "../utils/utils-library.json"
  },
  "$body": { "$defRef": "note_block_sound" },
  "$templates": {},
  "$definitions": {
    "note_block_sound": { 
      "$type": "union",
      "options": [{"$ref": "utils:resource_location", "schema": "sound_event", "description": "The ID of the sound event played by a note block when this player head is placed above."}]
    }
  }
}